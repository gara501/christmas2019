import React from 'react';
const Error = ({message}) => (
  <div className="nes-container is-rounded is-error error-message">
    <span className="error-message">{message}</span>
  </div>
  
);
 
export default Error;